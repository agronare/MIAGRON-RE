# ⚠️ ESTE ARCHIVO ES UNA PLANTILLA - NO CONTIENE VALORES REALES
# Copia este archivo y configura las variables en Vercel Dashboard

# Database (usar PostgreSQL en producción - ej: Neon, Supabase, Railway)
DATABASE_URL="postgresql://user:password@host:5432/database?schema=public"

# Server
PORT=4000
NODE_ENV=production

# JWT (generar secreto seguro: openssl rand -base64 32)
JWT_SECRET=tu-secreto-jwt-muy-seguro-cambiar-esto
JWT_EXPIRES_IN=7d

# CORS (URLs de tus frontends desplegados en Vercel)
CORS_ORIGINS=https://tu-frontend.vercel.app,https://tu-portal.vercel.app

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX=200

# File Upload
UPLOAD_DIR=./uploads
MAX_FILE_SIZE=10485760

# Invoice Storage
INVOICE_STORAGE_PATH=./uploads/invoices

# FacturAPI (PAC Provider para CFDI) - Credenciales de producción
CFDI_PAC_PROVIDER=facturapi
CFDI_PAC_API_KEY=sk_live_xxxxx
CFDI_PAC_SECRET=xxxxx
CFDI_MODO=production

# Datos fiscales de la empresa (Emisor)
COMPANY_RFC=AGR230616K40
COMPANY_RAZON_SOCIAL=AGRONARE
COMPANY_REGIMEN_FISCAL=601
COMPANY_CP=58880
